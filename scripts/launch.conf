# Default configuration for launch.sh
# This file is sourced if it exists in the same directory as launch.sh

SUPPORTED_EDITORS=("code" "cursor")

# Default editor (code, cursor, etc.)
DEFAULT_EDITOR="code"

# Default host for remote modes
DEFAULT_HOST="Fusion"

# Default dev container path for local-container mode
DEFAULT_DEV_CONTAINER="/opt/workspace/dev-containers/zephyr"

# Default workspace path
DEFAULT_WORKSPACE="/opt/workspace"

# Default SSH port
DEFAULT_SSH_PORT=22

# Default SSH key path (leave empty for default)
DEFAULT_SSH_KEY=""
